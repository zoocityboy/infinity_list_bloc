import 'package:flutter/material.dart';

class ItemErrorPlaceholder extends StatelessWidget {
  const ItemErrorPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile();
  }
}
